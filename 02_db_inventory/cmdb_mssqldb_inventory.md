I want you to act as an expert programmer and write a powershell script that does the following:

* connects to SCOM (__Microsoft System Center Operations Manager__) server
* queries available data about all SQL Server instances (we will use filters to get only some chuncks of information later on)
* create a JSON file (we will also see its structure later on) to feed ServiceNOW CMDB

